import vaultAbi from "../abi/vaultAbi.json";
import vaultFactory from "../abi/vaultFactoryAbi.json";

export const VAULT_CONTRACT = {
    address: "",
    abi: vaultAbi
}

export const FACTORY_VAULT = {
    address: "",
    abi: vaultFactory
}